export interface EcosystemEntry {
  isim: string;
  logo?: string | null;
  aciklama: string;
  webSitesi: string;
}

export const girisimler: EcosystemEntry[] = [];

export const kurumlar: EcosystemEntry[] = [];
